import { BorderStatusDirective } from './border-status.directive';

describe('BorderStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderStatusDirective();
    expect(directive).toBeTruthy();
  });
});
